# Copy to .env and set a token if you want AI semantic analysis.
# Without this token, the tool still runs in heuristic mode.
# Generate from host with: gh auth token
GITHUB_TOKEN=

# Optional concurrency override.
WHATIDID_ANALYZE_WORKERS=5
